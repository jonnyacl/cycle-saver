package com.cycle_saver.controller;

public class BaseController {

    public void checkAuth(String jwtToken) {
        // decode token and check user auth pass
        // if fails, throw error and
    }
}
